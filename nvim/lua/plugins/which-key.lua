return function()
  require("which-key").setup {
    -- triggers = {
    --   -- { "<auto>", mode = "nixsotc" },
    --   -- { "<auto>", mode = "nixsotc" },
    --   -- { "a", mode = { "n", "v" } },
    --   -- { "<leader>", mode = { "n", "v" } },
    -- }
  }
end
